# 更新记录

## 0.1.9 — 2026-09-08

- Hysteria2 与 Trojan 同机部署共用证书，统一申请、续期和服务重载；已有节点可迁移到共享证书。
- 修复只修改服务器名称、分组或 SSH 端口时误清除 Hook 证书指纹的问题。
- 任务历史清理保留排队和执行中的任务，避免丢失日志、状态及同机部署互斥保护。
- 部署失败时停止新服务，清除本次创建的配置，恢复原配置、文件权限、符号链接及原有运行状态。
- 解除 nftables 封禁时精确匹配地址、端口、协议和 DROP 规则；防火墙命令失败会反馈为任务失败。
- 批量封禁和解封先检查全部节点与服务器，避免请求被拒绝时仍留下部分已启动任务。
- 更新生产和构建依赖，升级 Vite、Electron Builder，并固定存在安全公告的间接依赖到修复版本。
- macOS 构建显式使用 ad hoc 签名，避免打包工具跳过签名而留下无效的包签名；此签名不等同于 Apple 公证。

升级后，请在“服务器”页升级远端 Hook，以启用部署回滚、封禁和共享证书修复。已有双协议服务器还需重新部署其中一个节点才能迁移旧证书配置；详见 README 的“同机部署 Hysteria2 和 Trojan”。

构建工具中的 `extract-zip@2.0.1` 尚无修复版本。本项目通过 pnpm 补丁拒绝指向解压目录外的符号链接，检查已有链接，并拒绝覆盖链接目标；补丁由 `scripts/extract-zip.test.cjs` 验证，正常的 Electron framework 相对链接仍受支持。依赖审计按版本识别时仍会报告 [GHSA-jmr9-qjv8-65gv](https://github.com/advisories/GHSA-jmr9-qjv8-65gv)，本项目未屏蔽该公告。
